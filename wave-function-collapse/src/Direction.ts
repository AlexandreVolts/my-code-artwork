export enum Direction {
  RIGHT = 0,
  BOTTOM = Math.PI / 2,
  LEFT = Math.PI,
  TOP = Math.PI + Math.PI / 2,
}

export const dirs: Direction[] = [
  Direction.RIGHT,
  Direction.BOTTOM,
  Direction.LEFT,
  Direction.TOP,
];
